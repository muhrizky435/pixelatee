import { NavBar } from "../components/NavBar";

export default function About() {
  return (
    <header className="font-default">
      <NavBar backgroundColor="bg-primary" textColor="text-secondary"></NavBar>
    </header>
  );
}
